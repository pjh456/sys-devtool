# pyproject.toml
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "greetlab-25020007100"
version = "0.1.0"
[project.scripts]
sdt-greet = "greetlab.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
